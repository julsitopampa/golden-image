[X] Set up self-hosted runner to interact with proxmox
[] Set up Ci workflows to test and then build image
[] Gestion comunicator : 
    - provision d'un compte provisioner et mdp générique avec preseed.cfg
    - utiliser par packer ssh_username ; ssh_password
    - utiliser par cloud-init puis supprimer et ajout clé publique